import styled from "styled-components";

export const TasksLeftStyle = styled.div`
    font-size: ${({ theme }) => theme.fontSizes.xxxl};
    color: ${({ theme }) => theme.colors.gray_darken};
    margin: ${({ theme }) => theme.margins.default};
`;
